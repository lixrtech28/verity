[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "opportunity-intelligence-agent"
version = "0.1.0"
description = "Evidence-disciplined opportunity extraction from approved public sources"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8,<9"]
